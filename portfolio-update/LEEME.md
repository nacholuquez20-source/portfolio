# Portfolio — versión actualizada (Quebrada del Portugués)

Reemplazá estos archivos en tu carpeta local "Portfolio" (los viejos se sobreescriben):

- index.html      (reemplaza)
- style.css       (reemplaza)
- assets/Foto.jpg (reemplaza — recortada sin franjas negras)
- main.js         (reemplaza — ahora vacío, la lógica está en app.jsx)
- app.jsx         (NUEVO)
- tweaks-panel.jsx(NUEVO)

No toques Dockerfile, railway.json ni README.md — siguen sirviendo.

Luego:
  git add .
  git commit -m "rediseño: Quebrada del Portugués"
  git push

Railway va a redeployar solo.
